---
export const prerender = true;

import Layout from '../layouts/Layout.astro';
import { services, site } from '../data/hvac-site-data';
---

<Layout
  title={`HVAC services | ${site.name}`}
  description="Air conditioning, heating, maintenance, and commercial HVAC for Cleveland and western suburbs. Repairs, replacements, and tune-ups."
  currentPath="/services"
>
  <main>
    <section class="w-full border-b border-border bg-background">
      <div class="mx-auto grid w-[90%] max-w-none grid-cols-12 gap-6 py-16 md:gap-8 md:py-20">
        <div class="col-span-12 lg:col-span-8">
          <p class="text-[11px] font-medium uppercase tracking-[0.14em] text-foreground-muted">Services</p>
          <h1 class="mt-4 text-[clamp(2rem,4vw,3.25rem)] font-semibold leading-[1.02] tracking-[-0.04em]">
            HVAC for homes and businesses west of the Cuyahoga
          </h1>
          <p class="mt-6 max-w-prose text-base leading-relaxed text-foreground-muted">
            Whether you need a same-day refrigerant leak found or a furnace swap before the lake-effect cold sets in, we
            structure visits so you know what failed, what it costs, and what happens next.
          </p>
        </div>
        <div class="col-span-12 flex items-end lg:col-span-4">
          <button
            type="button"
            class="inline-flex min-h-11 w-full cursor-pointer items-center justify-center rounded-full bg-inverse px-4 text-[0.95rem] font-medium text-inverse-foreground md:w-auto"
            data-drawer-open
          >
            Book or ask a question
          </button>
        </div>
      </div>
    </section>

    <section class="w-full bg-background">
      <div class="mx-auto w-[90%] max-w-none divide-y divide-border py-12 md:py-16">
        {services.map((service) => (
          <article id={service.slug} class="grid grid-cols-12 gap-6 py-12 md:gap-10 md:py-16">
            <div class="col-span-12 md:col-span-5">
              <h2 class="text-2xl font-semibold tracking-[-0.035em] md:text-3xl">{service.name}</h2>
            </div>
            <div class="col-span-12 md:col-span-7">
              <p class="text-base leading-relaxed text-foreground-muted">{service.summary}</p>
              <ul class="mt-6 grid gap-2 text-sm text-foreground-muted">
                <li class="flex gap-2">
                  <span class="text-foreground" aria-hidden="true">—</span>
                  Upfront scope: diagnosis first, then options—not a laundry list of upsells.
                </li>
                <li class="flex gap-2">
                  <span class="text-foreground" aria-hidden="true">—</span>
                  Parts common to Cleveland installs stocked on trucks when possible.
                </li>
                <li class="flex gap-2">
                  <span class="text-foreground" aria-hidden="true">—</span>
                  Paper trail: model/serial photos and filter size on the invoice when relevant.
                </li>
              </ul>
            </div>
          </article>
        ))}
      </div>
    </section>
  </main>
</Layout>
